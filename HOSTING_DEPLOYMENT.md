# 📋 Инструкция по развертыванию на хостинге

## 1️⃣ Подготовка проекта

### Обновление API URLs
В файле `/src/components/OrderForm/OrderForm.jsx` обновите URL бэкенда:

```javascript
// Замените:
const response = await fetch('https://express-kkhanill.vercel.app/api/tbank/init-payment', {

// На:
const response = await fetch('https://ваш-домен.com/api/tbank/init-payment', {
```

Также обновите в других компонентах, где используются API запросы (Presale, etc).

## 2️⃣ Сборка проекта

```bash
npm run build
```

Будет создана папка `build/` со всеми файлами для развертывания.

## 3️⃣ Загрузка на хостинг

### Через FTP/SFTP:

1. Подключитесь к серверу через FTP клиент (FileZilla, WinSCP и т.д.)
2. Загрузите содержимое папки `build/` в корневую папку хостинга (обычно `public_html/`)
3. Убедитесь, что файл `.htaccess` загружен в корень
4. Убедитесь, что в корне находится файл `index.html`

### Через панель управления хостингом:

1. В панели хостинга загрузите файлы через файловый менеджер
2. Распакуйте архив в корневую папку

## 4️⃣ Настройка .htaccess

Убедитесь, что на хостинге включены следующие модули Apache:
- `mod_rewrite` - для переписывания URL
- `mod_headers` - для установки заголовков (опционально)

Если `.htaccess` не работает, обратитесь к поддержке хостинга.

## 5️⃣ Проверка конфигурации

После загрузки:

✅ Откройте https://ваш-домен.com/ - должна загрузиться главная страница
✅ Перейдите на https://ваш-домен.com/course - должна работать навигация
✅ Перейдите на https://ваш-домен.com/presale - должна работать навигация
✅ Откройте DevTools (F12) - проверьте Console на ошибки

## 6️⃣ Обновление бэкенда API URLs

Если у вас есть собственный бэкенд на том же домене, убедитесь, что:

1. API доступен по адресу: `https://ваш-домен.com/api/tbank/init-payment`
2. CORS настроен правильно для вашего домена
3. Переменные окружения установлены на сервере

## 7️⃣ Кэширование

Для лучшей производительности, добавьте в `.htaccess`:

```apache
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/html "access plus 1 day"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType image/* "access plus 1 year"
</IfModule>
```

## 8️⃣ Проблемы и их решение

### "Page not found" при переходе на подстраницы
- Убедитесь, что `.htaccess` загружен и модуль `mod_rewrite` включен
- Проверьте права доступа на файлы (644 для файлов, 755 для папок)

### "Cannot POST /api/..."
- Убедитесь, что API доступен и правильно настроен
- Проверьте URL в компонентах (замените на правильный домен)
- Проверьте CORS заголовки

### Стили не загружаются
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Проверьте, что все файлы загружены в `build/`

---

**После выполнения этих шагов ваш сайт должен работать на собственном хостинге! 🚀**
