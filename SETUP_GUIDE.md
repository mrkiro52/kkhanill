# 🚀 Kkhanill - Курс "Как начать работать на себя в IT"

Полнофункциональный сайт с продажей онлайн-курса, интеграцией платежей через ТБанк и системой предзаписи.

## 📁 Структура проекта

```
kkhanill/
├── src/
│   ├── components/          # React компоненты
│   │   ├── OrderForm/      # Форма заказа с платежами
│   │   ├── GiftButton/     # Кнопка подарка
│   │   ├── VideoModal/     # Модал для видео
│   │   └── ...
│   ├── pages/              # Страницы приложения
│   │   ├── Home/           # Главная страница
│   │   ├── CourseContent/  # Содержание курса
│   │   ├── Presale/        # Страница предзаписи
│   │   ├── Success/        # Страница успеха платежа
│   │   ├── Fail/           # Страница ошибки платежа
│   │   └── ...
│   ├── config/             # Конфигурация
│   │   └── api.js          # API endpoints
│   ├── App.jsx             # Главный компонент
│   └── index.js            # Точка входа
├── public/
│   ├── index.html          # HTML шаблон
│   └── .htaccess          # Конфигурация Apache
├── build/                  # Собранный проект (после npm run build)
├── backend/                # Бэкенд на Vercel
│   ├── api/
│   │   ├── create-payment.js
│   │   ├── presale-signup.js
│   │   └── payment-notify.js
│   └── vercel.json
├── package.json
└── .env                    # Переменные окружения
```

## 🛠️ Установка и разработка

### Требования
- Node.js 14+
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Локальная разработка

```bash
# Запустить React приложение
npm start

# В другом терминале запустить Express сервер (если нужен)
npm run server

# Или оба одновременно
npm run dev
```

Приложение откроется на http://localhost:3000

## 🔨 Сборка для production

```bash
npm run build
```

Будет создана папка `build/` с оптимизированными файлами для развертывания.

## 📦 Развертывание

### На собственном хостинге

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Загрузите файлы на хостинг:**
   - Загрузите содержимое папки `build/` в корневую папку вашего хостинга (обычно `public_html/`)
   - Убедитесь, что файл `.htaccess` находится в корне

3. **Обновите API URLs:**
   
   Отредактируйте `.env` файл:
   ```
   REACT_APP_API_BASE_URL=https://ваш-домен.com
   ```
   
   Или обновите URL прямо в `/src/config/api.js`

4. **Проверьте работу:**
   - Откройте https://ваш-домен.com/
   - Проверьте навигацию между страницами
   - Тестируйте форму заказа

### На Vercel (для бэкенда)

Бэкенд уже развернут на https://express-kkhanill.vercel.app

Основные endpoints:
- `POST /api/tbank/init-payment` - инициализация платежа
- `POST /api/tbank/get-state` - получение статуса платежа
- `POST /api/presale-signup` - добавление в Google Sheets

## 🔑 Конфигурация

### Переменные окружения

Создайте файл `.env` (или используйте `.env.example` как шаблон):

```env
# API URL бэкенда
REACT_APP_API_BASE_URL=https://ваш-домен.com

# Или для локальной разработки:
REACT_APP_API_BASE_URL=http://localhost:5000
```

## 💳 Интеграция платежей (ТБанк)

### Настройка на бэкенде

Убедитесь, что в Vercel установлены переменные:
- `tbank_terminal_key` - ваш терминал ТБанк
- `tbank_password` - пароль ТБанк

### Тестирование платежей

1. Откройте форму заказа на сайте
2. Заполните данные
3. Нажмите "Получить доступ"
4. Вы будете перенаправлены на форму оплаты ТБанк
5. Используйте тестовые карты для проверки

## 🔄 Роутинг

Сайт использует `BrowserRouter` для стандартного роутинга:

```
/ - Главная страница
/course - Содержание курса
/presale - Предзапись на курс
/politics - Политика конфиденциальности
/public-offer - Публичная оферта
/success - Успешный платеж
/fail - Ошибка платежа
```

## 📱 Адаптивный дизайн

Сайт полностью адаптирован для всех устройств:
- Desktop (1920px+)
- Tablet (768px - 1024px)
- Mobile (< 768px)

## 🎨 Стили

Используется SASS для стилизации. Основные файлы:
- `/src/App.css` - глобальные стили
- `/src/fonts.css` - подключение шрифтов
- Каждый компонент имеет свой SCSS файл

## 📊 Google Sheets интеграция

Форма предзаписи отправляет данные в Google Sheets через Apps Script.

Данные, которые записываются:
- Имя
- Телефон
- Email
- Telegram
- Временная метка
- Тип заявки (presale)

## ⚙️ Troubleshooting

### "Page not found" на подстраницах
- Убедитесь, что `.htaccess` загружен в корень
- Проверьте, что `mod_rewrite` включен на сервере
- Попробуйте перезагрузить сервер

### Платежи не работают
- Проверьте переменные окружения на Vercel
- Убедитесь, что API URL правильно обновлен
- Проверьте консоль браузера (F12) на ошибки CORS

### Стили не загружаются
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Убедитесь, что все файлы загружены в `build/`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все файлы загружены на сервер
3. Проверьте логи сервера (если доступны)

## 📄 Лицензия

Приватный проект.

---

**Последнее обновление:** 12 декабря 2025
